[project]
name = "gpt-cloudmusic-control"
version = "0.2.0"
description = "A local MCP server for controlling the official NetEase Cloud Music Windows client."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.27,<2",
    "websocket-client>=1.8,<2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
